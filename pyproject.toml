[project]
name = "squadcast-migrator"
version = "0.1.0"
description = "Migration framework to migrate OpsGenie/PagerDuty to Squadcast"
requires-python = ">=3.8"
dependencies = [
    "requests==2.31.0",
    "python-dotenv==1.0.0",
    "click==8.1.3",
    "pydantic==2.0.3",
    "tqdm==4.66.1",
]
